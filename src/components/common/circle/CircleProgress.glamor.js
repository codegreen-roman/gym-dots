import { css } from 'glamor'

export const circleProgressWrapper = css({
    position: 'relative',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center'
})

export const circleInnerWrapper = css({
    position: 'absolute'
})
